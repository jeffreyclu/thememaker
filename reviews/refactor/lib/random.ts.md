# Refactor plan — `src/lib/random.ts`

**Non-comment LOC: 10.** Verdict vs ≤200: **PASS.**

Tiny pure RNG helpers (`randomHexColor`/`randomMode`/`randomInt`). No split, no duplication, no long functions.

## Ordered steps
No action required.
