# Refactor plan — `src/lib/color-names.ts`

**Non-comment LOC: 34.** Verdict vs ≤200: **PASS.**

Offline deterministic color naming (`hueFamily` + `describeColor`). Pure, tested (`color-names.test.ts`), tiny. No split, no duplication, no long functions.

## Ordered steps
No action required.
