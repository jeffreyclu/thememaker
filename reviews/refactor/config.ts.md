# Refactor plan — `src/config.ts`

**Non-comment LOC: 11.** Verdict vs ≤200: **PASS.**

Static product config: the `modes` list + `MAX_HISTORY`. Trivial. No split, no duplication, no long functions.

## Ordered steps
No action required.
