# Review: src/popup/components/Controls.tsx

**LOC**: 19 (under 200)

Thin connected component: pulls `mode`/`intensity`/`invert` from context, wires three pure form primitives on a `value`/`onChange` contract. Textbook presentational/connected split — zero business logic, memoized. Header comment is accurate.

## Findings

No findings. Clean, idiomatic, correctly layered.
